# Other image classifier fixtures

Place representative, anonymised image copies in the folder matching their
expected classification:

- `expected_photos/`
- `expected_infographics/`
- `expected_text_screenshots/`

The classifier test automatically checks every supported image in these folders.
